from itertools import combinations

def solution(number):   
    threePeople = 0
    for i in list(combinations(number,3)):
        if sum(i) == 0:
            threePeople += 1
    
    return threePeople
